export interface Person {
  name: String;
  age: Number;
  country: String;
}

export interface NavItem {
  label: string;
  path: string;
}

export interface LoginForm {
  email: string;
  pwd: string;
}